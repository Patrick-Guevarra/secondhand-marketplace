@model List<UniThrift.Models.Listing>

@{
    ViewData["Title"] = "Browse Listings";
}

<h1>Shop</h1>
<h2>Latest Items</h2>

<div class="listing-grid">
@foreach (var item in Model)
{
    <div class="listing-card">
        <h3>@item.Title</h3>
        <p>@item.Category</p>
        <p><strong>$@item.Price</strong></p>
        <p>@item.Description</p>
    </div>
}
</div>

